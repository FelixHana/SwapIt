<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cswap.auth.mapper.SysNodeMapper">

    <select id="listRolePerms" resultType="com.cswap.auth.domain.po.SysNode">
        SELECT n.id,
               n.name,
               n.url,
               n.method,
               GROUP_CONCAT(r.role_name SEPARATOR ',') AS roles
        FROM sys_node n
                 LEFT JOIN
             sys_role_node rn ON n.id = rn.node_id
                 LEFT JOIN
             sys_role r on rn.role_id = r.id
        GROUP BY n.id, n.name, n.url, n.method
    </select>


</mapper>